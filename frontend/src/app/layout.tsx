import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Saudi Market AI - Feasibility Study Platform",
  description: "AI-powered feasibility studies for Saudi Arabian entrepreneurs. Test your business ideas with virtual audiences, RAG-based analysis, and competitor research.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
